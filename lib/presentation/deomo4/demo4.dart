import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Serchsss extends StatelessWidget {
  const Serchsss({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        actions: [
          Row(
            children: [

            ],
          )
        ],
      ),
      body: Column(
        children: [
          Text("data")
        ],
      ),
    );
  }
}
